import { blurhashToDataUri } from "@unpic/placeholder";

/**
 * Placeholder blurhash usado como background enquanto avatares/capas carregam.
 * Decodificado uma única vez para data-URI (era o `blurDataURL` do @son426/vite-image).
 */
export const AVATAR_BLUR = blurhashToDataUri("LKO2:N%2Tw=w]~RBVZRi};RPxuwH");
